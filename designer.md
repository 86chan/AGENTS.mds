# 設計書運用ガイドライン

> **目的**: 画面仕様と業務仕様を分離し、AIと人間の開発効率を高める。
> **前提**: 設計書は業務の決めごとを書く場所であり、実装の設計図ではない。

---

## 1. 記述の鉄則（全ドキュメント共通）

* **長文の禁止**: 1文あたり80文字前後を超える文章は禁止する。
  * **カウント基準**: 全角・半角を問わず1字1文字と数える。
  * **対象外**: 表のセル、コードブロック、URLを含む行は数えない。
* **箇条書きの徹底**: 複雑な説明は段落にせず、箇条書きで表現する。
* **簡潔な表現**: 主語と述語を明確にし、冗長な装飾語は削る。
* **重複の禁止**: 同じ規約を2ファイルに書かない。共通仕様書へ切り出す。
* **参照の優先**: 他文書の内容は再掲せず、IDとリンクで参照する。

### 1.1 共通の記載事項

* **見出し**: `# [ID] 名称` を1行目に置く。
* **冒頭**: 文書状態を書く（`確定` または `検討中`）。
* **日付**: 文書に書かない。更新日はGitの履歴で追う。
* **決定者**: 業務仕様書には、その決めごとを決めた部門または担当を書く。
* **未決事項**: 決まっていない論点は明記する。空欄のまま残さない。
* **構成**: 見出しは書く内容に合わせて立てる。定型の雛形は用意しない。

### 1.2 使う言葉

普段の業務で使われている言葉だけを使う。新しい言葉を作らない。

* **語彙の出どころ**: 用語集（`DEF-000`）に載っている言葉から選ぶ。
* **造語の禁止**: 辞書にない熟語を作らない。漢語を3つ以上つなげない。
* **和語の優先**: 和語で言えることをカタカナ語にしない。
* **カタカナ語の制限**: 業務で口に出して使う言葉だけ許す。3語続いたら書き直す。
* **言い換えの禁止**: 担当者が「返品」と呼ぶものを「返送処理」と書かない。
* **冗長表現の削除**: 「〜という形で」「〜に関して」「〜の観点から」を消す。
* **短縮**: 「〜することができる」は「〜できる」と書く。
* **記号**: 全角ダッシュ（——）は使わない。カッコか読点に置き換える。

**言い換え表**

| 使わない             | 使う            |
| :------------------- | :-------------- |
| 具備する / 保持する  | 持つ / 備える   |
| 実施する / 遂行する  | 行う            |
| 〜を担保する         | 〜を守る        |
| 〜を勘案する         | 〜を考える      |
| 当該〜               | この〜 / その〜 |
| 〜における           | 〜での          |
| 保守性向上化         | 直しやすくする  |
| ユーザー体験の最適化 | 使いやすくする  |
| 逸脱ケース           | 例外            |
| 恒常的に             | いつも / 常に   |

* **確認方法**: 業務担当者に声に出して読んでもらい、引っかかる語を書き直す。

---

## 2. 用語集（DEF-000）

用語集は設計書として持つ。本ガイドラインには用語を書かない。

* **位置づけ**: 最初に作る設計書。画面仕様書と業務仕様書より先に作る。
* **場所**: `docs/DEF-000_用語集.md`。docs直下に置く。IDは `000` で固定する。
* **載せるもの**:
  * 業務の言葉（会員ランク、返品、締め日など）。
  * 業務上の状態の呼び名（有効、ロック中など）。
  * 設計書を書くための言葉（決めごと、判断の基準、根拠など）。
  * 文書状態の値（`確定`、`検討中`）。この2語以外を文書状態に使わない。
* **載せないもの**:
  * 実装の名前（テーブル名、クラス名、API名）。
  * 国語辞書で足りる一般語。
* **1語の書式**: 用語、定義、使わない言い換え、の3列で書く。
* **使わない言い換え**: 同じものを指す別名を必ず書く。これが表記ゆれの歯止めになる。
* **機械可読の維持**: 「使わない言い換え」の列は禁止語チェックの元にする。書式を崩さない。
* **追加のタイミング**: 新しい言葉を使う前に用語集へ足す。仕様書の中で定義しない。
* **行数上限**: 対象外とする。分割せず1ファイルで持つ。

### 2.1 最初に作らせるとき

AIに用語集を作らせる場合、次の順で進める。

1. 業務担当者の議事録、既存の画面、問い合わせ文面から言葉を拾わせる。
2. 同じものを指す別名をまとめさせ、どれを使うか担当者に決めてもらう。
3. 決まった語を用語集に、残りを「使わない言い換え」の列に入れる。
4. 以降の仕様書作成では、用語集を必ず添えて渡す。

---

## 3. 書くことと書かないこと

設計書には概念レベルの決めごとを書く。実装の手順は書かない。

| 書くこと                 | 書かないこと                |
| :----------------------- | :-------------------------- |
| 業務上の判断と、その基準 | 関数名、引数、戻り値        |
| その基準を選んだ根拠     | テーブル名、カラム名、API名 |
| 境界の値と、その値の根拠 | 条件分岐の順序、データ構造  |
| 例外の扱い               | エラーコード、ログ出力形式  |
| 状態と、変わるきっかけ   | クラス構成、実装の分け方    |

* **見分け方**: 実装のやり方を変えても、仕様書の書き直しが要らないか。
* **状態の呼び方**: 用語集の語で書く。コード上の定数名に合わせない。
* **図の線引き**: 業務レベルの状態遷移図は書いてよい。ER図など実装寄りの図は書かない。

### 3.1 記述例

**NG（実装レベル）**

```text
calculateDiscount(rank, amount) を呼び出す。
rank が "GOLD" の場合、amount * 0.9 を返す。
```

**OK（概念レベル）**

```text
* ゴールド会員には10%の割引を適用する。
  * 根拠: 年間20万円以上の購入を続けてもらうための優遇。
  * 併用: 金額による割引と重ねて適用してよい。
```

---

## 4. 分量の上限

ファイルが太るのを、読んだ印象ではなく行数で止める。

| 種別       | 目安  | 上限  | 超過時の対応                            |
| :--------- | :---: | :---: | :-------------------------------------- |
| 画面仕様書 | 150行 | 250行 | 決めごとをBIZへ切り出す／画面を分割する |
| 業務仕様書 | 100行 | 200行 | 決めごとの単位で分割する                |
| 共通仕様書 | 200行 | 300行 | 分類を細かく分ける                      |

* **測定**: `wc -l docs/**/*.md` で確認する。
* **原則**: 上限を超えたら「書きすぎ」ではなく「分割漏れ」と考える。
* **例外**: 分けると意味が壊れる場合のみ、冒頭に理由を1行で書く。
* **対象外**: 本ガイドライン、`INDEX.md`、用語集（`DEF-000`）、`.uml` などの図ファイル。

---

## 5. 粒度の決め方

### 5.1 画面（SCR）

* **単位**: 利用者が1つの画面と認識する単位ごとに、1ファイルとする。
* **Web画面**: 原則として、URLで直接開ける単位を1画面とする。
* **URL中の値**: 注文番号などの値が違うだけなら、同じ画面とする。
* **表示条件**: URLの検索条件や表示条件が違うだけなら、同じ画面とする。
* **モーダル・タブ**: 原則として親画面に含める。
* **分けてよい場合**: 単独の業務目的を持つ場合だけ、別画面として扱う。

### 5.2 決めごと（BIZ）

* **単位**: 業務担当者が「1つの決めごと」と認識するまとまり。
* **見分け方**: そのファイルだけを担当者に見せて、合っているか判断してもらえるか。
* **大きすぎのサイン**: 概要が「〜と〜を行う」と接続詞でつながる。
* **細かすぎのサイン**: 単独では意味をなさず、他と必ずセトで読む。
* **注意**: 実装の分け方に合わせない。コードの構成が変わっても分け直さない。

---

## 6. 基本方針

画面（UI）、業務の決めごと、全体規約の3層で管理する。

| 種別           | 配置先           | 主な役割                           | 記述しないこと           |
| :------------- | :--------------- | :--------------------------------- | :----------------------- |
| **画面仕様書** | `docs/screens/`  | UI/UX、レイアウト、表示制御、遷移  | 決めごと、データの扱い   |
| **業務仕様書** | `docs/business/` | 決めごと、判断の基準、状態、例外   | デザイン情報、実装の手順 |
| **共通仕様書** | `docs/common/`   | 全体規約、共通の表示ルール         | 個別の画面・業務の事情   |

* **共通仕様書に置くもの**: 入力チェックの書式、エラー表示の統一ルール、権限の区分、日付や金額の書き方。
* **用語集の扱い**: 内容は共通層に属するが、ファイルは `docs` 直下に固定で置く（§2）。
* **移す目安**: 3ファイル以上に同じ記述が出たら共通仕様書へ移す。

---

## 7. ファイル命名規則

### 7.1 フォーマット

* **画面仕様書**: `SCR-{3桁数字}_{画面名}.md`
* **業務仕様書**: `BIZ-{3桁数字}_{業務名}.md`
* **共通仕様書**: `COM-{3桁数字}_{分類名}.md`
* **用語集**: `DEF-000_用語集.md`

### 7.2 命名ルール

* **ID指定**: 3桁の連番（`001`, `002`, ...）を使用する。
* **予約ID**: `DEF-000` は用語集に固定する。他に割り当てない。
* **区切り文字**: IDと名称の間はアンダースコア `_` で区切る。
* **ファイル名**: スペースを含めず、業務で通じる日本語の名詞にする。
* **動詞の回避**: 「〜処理」「〜判定」より「〜ルール」「〜基準」を選ぶ。
* **ID再利用の禁止**: 廃止したIDは欠番のまま残し、新しく割り当てない。
* **例**:
  * 画面: `SCR-001_ログイン画面.md`
  * 業務: `BIZ-001_アカウントロック基準.md`
  * 共通: `COM-001_入力チェック規約.md`

---

## 8. ディレクトリ構造

```text
docs/
├── INDEX.md          # 全仕様書の一覧と関連（逆引き）
├── DEF-000_用語集.md  # 用語集（docs直下に固定）
├── screens/          # 画面仕様書 (UI/操作感)
│   ├── SCR-001_ログイン画面.md
│   ├── SCR-002_ダッシュボード画面.md
│   └── res/          # 仕様書以外のファイル
│       ├── img/      # 画像
│       └── uml/      # UML等の図ソース (.uml)
├── business/         # 業務仕様書 (業務の決めごと)
│   ├── BIZ-001_アカウントロック基準.md
│   └── BIZ-002_割引適用ルール.md
├── common/           # 共通仕様書 (全体規約)
│   ├── COM-001_入力チェック規約.md
│   └── res/          # 複数種別で共有する図・画像
├── archive/          # 廃止済み仕様書
└── AGENTS.md         # 本ガイドライン
```

### 8.1 仕様書以外のファイル（図・画像）

画像やUML図など、Markdown以外のファイルの置き場所と取り込み方を定める。

* **置き場所**: 種別ごとのディレクトリ（`screens/` 等）の直下に `res/` を作って置く。
* **共通図**: 複数種別で使う図は `common/res/` に置く。
* **画像**: `res/img/` に置く。
* **図ソース**: UML等は `res/uml/` に `{図の概要}.uml` の形で1図1ファイルで置く。
* **取り込み**: Markdownには図を貼らず、取り込みディレクティブで参照する（`!include` / `@import`）。
* **分割の原則**: 図は必ず別ファイルにする。Markdown本文へ図の定義を直接書かない。
* **作る範囲**: 実際にファイルを置く種別にだけ `res/` を作る。空のまま用意しない。
* **参照パス**: 相対パスで書く（例: `res/uml/login_flow.uml`）。
* **ファイル名**: 仕様書のIDに合わせなくてよい。図の中身が分かる名前を付ける。

---

## 9. 参照ルール

* **SCR → BIZ / COM**: 関わる決めごとと規約をリンクで並べる。
* **BIZ → BIZ / COM**: 制限しない。ただし内容の再掲は禁止する。
* **BIZ → SCR**: 書かない。決めごとは画面の有無に左右されない。
* **全仕様書 → INDEX**: 末尾に戻りのリンクを置く。読み手が一覧へ帰れるようにする。
* **用語集への参照**: 個別には書かない。全仕様書に当然に適用する。
* **循環参照**: 許す。読み手は困らない。
* **禁じるのは重複だけ**: 同じ内容が2箇所にあると、片方が古くなる。
* **リンク形式**: 相対パスで書く。

### 9.1 逆引き（INDEX.md）

* **役割**: どの画面がどの決めごとを使うかを1枚で管理する。
* **理由**: 業務仕様書に画面IDを書くと、画面が増減するたびに業務仕様書を触ることになる。
* **主語の固定**: 1行を1画面（SCR）とする。関連するBIZ・COMを関連IDに列挙する。
* **形式**: 画面ID、画面名、文書状態、関連ID（BIZ/COM）の4列とする。
* **更新**: 新規作成、廃止、名称変更のたびに更新する。

---

## 10. 変更・廃止の運用

### 10.1 変更

* **順序**: 仕様を変えるときは、コードより先に仕様書を直す。
* **影響の確認**: 直す前に `grep -r "対象ID" docs/` と `INDEX.md` で関連を調べる。
* **言葉の変更**: 呼び名を変えるときは用語集を先に直し、旧称を「使わない言い換え」へ移す。
* **実装都合の変更**: コードの構成を変えただけでは仕様書を触らない。触る必要が出たら粒度を疑う。

### 10.2 廃止

廃止は文書状態では表さない。アーカイブへ移すことで表す。

* **手順**:
  1. `INDEX.md` で関連が0件であることを確認する。
  2. ファイルを `docs/archive/` へ移す。
  3. `INDEX.md` から該当行を消す。
* **IDの扱い**: 欠番のまま残し、使い回さない。
* **用語集**: 廃止しない。使わなくなった語は「使わない言い換え」に残す。

---

## 11. レビューチェックリスト

仕様書のマージ前に確認する。自動判定できる項目はツールに任せ、人は判断項目に集中する。

**自動チェック（lintで判定）**

- [ ] 行数が上限内か（`wc -l`）
- [ ] 1文が80文字以内か（表・コード・URL行を除く）
- [ ] 用語集の「使わない言い換え」が混ざっていないか（`DEF-000` から禁止語を生成し `grep`）
- [ ] 関数名、テーブル名、API名が混ざっていないか（`()` やcamelCaseを検出）
- [ ] 図の定義がMarkdown本文に直書きされていないか
- [ ] 文書状態が `確定` / `検討中` のどちらかか
- [ ] INDEX への戻りリンクが末尾にあるか

**目視チェック（人が判断）**

- [ ] 用語集に載っていない言葉を使っていないか
- [ ] 新しく使った言葉を用語集へ足したか
- [ ] 辞書にない造語や、業務で使わないカタカナ語がないか
- [ ] 業務担当者が読んで、合っているか判断できるか
- [ ] 判断の基準に、その値を選んだ根拠が書かれているか
- [ ] 画面仕様書に決めごとが混ざっていないか
- [ ] 業務仕様書にUI情報が混ざっていないか
- [ ] 他の仕様書と重複した記述がないか
- [ ] 未決事項を空欄で放置していないか
- [ ] `INDEX.md` を更新したか
